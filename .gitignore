/.quarto/
_book
_extensions/nmfs-opensci/titlepage
*_cache
*_files

figure
.DS_Store
.Rproj.user

tikz*log
_extensions
GSE*
.Rhistory

# LaTeX/PDF intermediates from `quarto render` of the book (index.qmd is the source, not an artifact)
index.tex
index.pdf
index.aux
index.log
index.toc
index.idx
index.lof
index.lot

# Generated at render time by reading_and_writing.qmd (download.file / write.csv / write_xlsx)
# Anchored to repo root so the vendored copy under data/ is still tracked.
/SaleData.xlsx
data.csv
data.xlsx
genes.csv
genes.xlsx

# Unused by the book (kept on disk, not part of the build)
bioc_data_access_packages.csv
workshop_cost_calculator.R

# quartobot citation pipeline: ignore the local CSL cache and backups.
# references.resolved.bib is committed (a stub when there are no PID keys), and
# _freeze/ stays committed per this repo's caching strategy — do not add either here.
references.json
*.bak-*

**/*.quarto_ipynb
.claude/

# Local Netlify folder
.netlify
